# Probe harness

Generated by `scripts/laws-inventory.py` from `docs/laws.json`. Do not hand-edit — see `docs/design.md#laws-inventory`.

One probe per tabulated law that has a C++ equation (`docs/laws.md`'s non-`—` rows). Each is checked by example with `std` types (`std::expected`-shaped probes, per `docs/design.md#cpp-counterpart`'s decision that the CRTP machinery itself does no law-checking); equality is `==` on the carrier after conversion. Nothing here runs C++: the corpus that does is `cpp/transpose/tests/beman/transpose/probe_harness.test.cpp`, one `TEST_CASE` per row below, named `probe-harness: <Module>.<theorem>` (`make cpp-probes`).

## `ap_pure_id` (Graded/Accum.lean)

    apply(pure(id), x) == x

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_pure_pure` (Graded/Accum.lean)

    apply(pure(f), pure(a)) == pure(f(a))

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_interchange` (Graded/Accum.lean)

    apply(u, pure(a)) == apply(pure([=](auto f){ return f(a); }), u)

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_comp` (Graded/Accum.lean)

    apply(apply(apply(pure(compose), u), v), w) == apply(u, apply(v, w))

check by example with std types; equality is `==` on the carrier after conversion.

## `errsOf_traverseK` (Graded/AccumTraverse.lean)

    traverse(f, xs, accumulating).error().witness<E>() == the leftmost failing f(x) of kind E, for every kind E that fails; succeeding positions contribute nothing  // per-kind form of 'every failing position contributes once, in source order': the C++ evidence collapses repeats of a kind to the leftmost, so source order survives only within a kind

check by example with std types; equality is `==` on the carrier after conversion.

## `traverseK_ok` (Graded/AccumTraverse.lean)

    traverse(f, xs) == pure(transform(xs, f))  // when every check succeeds, the accumulating form is just transform

check by example with std types; equality is `==` on the carrier after conversion.

## `toGraded_widen` (Graded/AccumTraverse.lean)

    widen<Es2>(x /*accumulated*/).error().witness<E>() == x.error().witness<E>() for every kind E, witness_count preserved  // the accumulating carrier must support subsumption at all - the model went fifteen steps without it because no law asked; in C++ both objects share one carrier, so the conversion is the same conversion

check by example with std types; equality is `==` on the carrier after conversion.

## `toGraded_apK` (Graded/AccumTraverse.lean)

    apply(f, x, accumulating).error().witness<E>() == apply(f, x).error().witness<E>() for the kind E of the short-circuiting error; equal outright when at most one side fails  // per-kind form, unconditional: the accumulating apply keeps the function's witness for its kind and the short-circuiting one keeps the function's error

check by example with std types; equality is `==` on the carrier after conversion.

## `toGraded_traverseK` (Graded/AccumTraverse.lean)

    traverse(f, xs, accumulating).error().witness<E>() == traverse(f, xs).error().witness<E>() for the kind E of the short-circuiting error; equal outright when exactly one position fails  // per-kind form: the C++ evidence keeps one left-biased witness per kind, so the model's first_error (head of a source-ordered list) is not computable from it -- docs/design.md#accumulated-evidence-shape

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_pure_id` (Graded/Applicative.lean)

    apply(pure(id), x) == x

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_pure_pure` (Graded/Applicative.lean)

    apply(pure(f), pure(a)) == pure(f(a))

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_interchange` (Graded/Applicative.lean)

    apply(u, pure(a)) == apply(pure([=](auto f){ return f(a); }), u)

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_comp` (Graded/Applicative.lean)

    apply(apply(apply(pure(compose), u), v), w) == apply(u, apply(v, w))

check by example with std types; equality is `==` on the carrier after conversion.

## `ap_flip` (Graded/Applicative.lean)

    apply(f, x) == apply_flipped(f, x)  // only when at most one side errs

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_map` (Graded/Compose.lean)

    transform(flatten(x), f) == flatten(transform(transform(x, f)))

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_pure_outer` (Graded/Compose.lean)

    flatten(pure(y)) == y

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_pure_inner` (Graded/Compose.lean)

    flatten(transform(x, pure)) == x

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_flatten` (Graded/Compose.lean)

    flatten(flatten(x)) == flatten(transform(x, flatten))

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_widen_outer` (Graded/Compose.lean)

    flatten(widen<Es2>(x)) == widen<Es2 | Fs>(flatten(x))

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_widen_inner` (Graded/Compose.lean)

    flatten(transform(x, widen<Fs2>)) == widen<Es | Fs2>(flatten(x))

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_comm` (Graded/Compose.lean)

    flatten(x) == flatten(swap(x))  // up to reordering the union's arguments

check by example with std types; equality is `==` on the carrier after conversion.

## `Comp.ap_pure_id` (Graded/ComposeApp.lean)

    apply(pure(id), x) == x  // nested, componentwise

check by example with std types; equality is `==` on the carrier after conversion.

## `Comp.ap_pure_pure` (Graded/ComposeApp.lean)

    apply(pure(f), pure(a)) == pure(f(a))  // nested, componentwise

check by example with std types; equality is `==` on the carrier after conversion.

## `Comp.ap_interchange` (Graded/ComposeApp.lean)

    apply(u, pure(a)) == apply(pure([=](auto f){ return f(a); }), u)  // nested

check by example with std types; equality is `==` on the carrier after conversion.

## `Comp.ap_comp` (Graded/ComposeApp.lean)

    apply(apply(apply(pure(compose), u), v), w) == apply(u, apply(v, w))  // nested

check by example with std types; equality is `==` on the carrier after conversion.

## `traverseComp_eq` (Graded/ComposeApp.lean)

    traverse_comp(a -> transform(f(a), k), xs) == transform(traverse(f, xs), ys -> traverse(k, ys))  // traverse_comp is a hand fold over the library's two applicative objects: a composed applicative cannot be a traverse policy, because applicative_value_t reads the carrier's value_type, and for a nested expected that is the inner carrier

check by example with std types; equality is `==` on the carrier after conversion.

## `flatten_ap` (Graded/ComposeApp.lean)

    flatten(apply(ff, xx)) == apply(flatten(ff), flatten(xx))  // one-sided condition only

check by example with std types; equality is `==` on the carrier after conversion.

## `joinAll_perm` (Graded/GradeFold.lean)

    error_set<Es...> == error_set<permutation of Es...>

check by example with std types; equality is `==` on the carrier after conversion.

## `joinAll_dedup` (Graded/GradeFold.lean)

    error_set<Es..., duplicates removed> == error_set<Es...>

check by example with std types; equality is `==` on the carrier after conversion.

## `bind_pure_left` (Graded/Monad.lean)

    and_then(pure(x), f) == f(x)

check by example with std types; equality is `==` on the carrier after conversion.

## `bind_pure_right` (Graded/Monad.lean)

    and_then(x, pure) == x

check by example with std types; equality is `==` on the carrier after conversion.

## `bind_assoc` (Graded/Monad.lean)

    and_then(and_then(x, f), k) == and_then(x, [=](auto a){ return and_then(f(a), k); })

check by example with std types; equality is `==` on the carrier after conversion.

## `bind_map` (Graded/Monad.lean)

    and_then(x, [=](auto a){ return pure(f(a)); }) == transform(x, f)

check by example with std types; equality is `==` on the carrier after conversion.

## `bind_widen` (Graded/Monad.lean)

    and_then(widen<Es2>(x), f) == widen<Es2 | Fs>(and_then(x, f))

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_map` (Graded/Morphism.lean)

    transform_error(transform(x, f), phi) == transform(transform_error(x, phi), f)

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_widen` (Graded/Morphism.lean)

    transform_error(widen<Es2>(x), phi) == widen<image of Es2>(transform_error(x, phi))

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_cast` (Graded/Morphism.lean)

    transform_error(x, phi) commutes with a same-set grade cast

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_pure` (Graded/Morphism.lean)

    transform_error(pure(a), phi) == pure(a)

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_bind` (Graded/Morphism.lean)

    transform_error(and_then(x, f), phi) == and_then(transform_error(x, phi), transform_error(_, phi) compose f)

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_ap` (Graded/Morphism.lean)

    transform_error(apply(f, x), phi) == apply(transform_error(f, phi), transform_error(x, phi))

check by example with std types; equality is `==` on the carrier after conversion.

## `rename_map2` (Graded/Morphism.lean)

    transform_error(map2(k, x, y), phi) == map2(k, transform_error(x, phi), transform_error(y, phi))

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_rename` (Graded/Morphism.lean)

    transform_error(traverse(f, xs), phi) == traverse(transform_error(f, phi), xs)

check by example with std types; equality is `==` on the carrier after conversion.

## `GradedHom.hom_ok` (Graded/Sufficient/MorphismK.lean)

    transform_error(ok(a), phi) == ok(a)  // at EVERY error set, not only the empty one where the pure law states it

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_nil` (Graded/Traverse.lean)

    traverse(f, {}) == pure({})

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_cons` (Graded/Traverse.lean)

    traverse(f, x :: xs) == map2(cons, f(x), traverse(f, xs))

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_map` (Graded/Traverse.lean)

    traverse(f, transform(xs, h)) == traverse(f compose h, xs)

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_fromEmpty` (Graded/Traverse.lean)

    static_assert(!traverse_accepts<fromEmpty, xs>)  // no C++ left side: at the empty grade the carrier is bare T, which is not a context, and expected<T, error_set<>> fails applicative_object's subsumption clause at its own grade; a no-fail traversal is spelled std::ranges::transform

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_length` (Graded/Traverse.lean)

    traverse(f, xs).value().size() == xs.size()  // shape preservation

check by example with std types; equality is `==` on the carrier after conversion.

## `sequence_cons` (Graded/Tuple.lean)

    transpose(x, xs...) == map2(tuple_cons, x, transpose(xs...))

check by example with std types; equality is `==` on the carrier after conversion.

## `bindF_pure_left` (Graded/Ungraded.lean)

    and_then(pure(x), f) == f(x)  // fixed grade, cast-free

check by example with std types; equality is `==` on the carrier after conversion.

## `bindF_pure_right` (Graded/Ungraded.lean)

    and_then(x, pure) == x  // fixed grade, cast-free

check by example with std types; equality is `==` on the carrier after conversion.

## `bindF_assoc` (Graded/Ungraded.lean)

    and_then(and_then(x, f), k) == and_then(x, [=](auto a){ return and_then(f(a), k); })  // fixed grade

check by example with std types; equality is `==` on the carrier after conversion.

## `apF_pure_id` (Graded/Ungraded.lean)

    apply(pure(id), x) == x  // fixed grade, cast-free

check by example with std types; equality is `==` on the carrier after conversion.

## `apF_pure_pure` (Graded/Ungraded.lean)

    apply(pure(f), pure(a)) == pure(f(a))  // fixed grade, cast-free

check by example with std types; equality is `==` on the carrier after conversion.

## `apF_interchange` (Graded/Ungraded.lean)

    apply(u, pure(a)) == apply(pure([=](auto f){ return f(a); }), u)  // fixed grade

check by example with std types; equality is `==` on the carrier after conversion.

## `apF_comp` (Graded/Ungraded.lean)

    apply(apply(apply(pure(compose), u), v), w) == apply(u, apply(v, w))  // fixed grade

check by example with std types; equality is `==` on the carrier after conversion.

## `traverse_fromEmpty_map` (Graded/Ungraded.lean)

    static_assert(!traverse_accepts<fromEmpty compose f, xs>)  // the same refusal as traverse_fromEmpty; the right side, transform(xs, f), is the only spelling the C++ has

check by example with std types; equality is `==` on the carrier after conversion.

## `sumEquiv_bindF` (Graded/Ungraded.lean)

    and_then(x, f) corresponds to std::expected/variant-style Sum::bind under the Equiv

check by example with std types; equality is `==` on the carrier after conversion.
