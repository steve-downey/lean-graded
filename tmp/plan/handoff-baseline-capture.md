# handoff → baseline-capture

Written by the planning consult. There is no previous step.

Goal and merge criterion: fixed by `step-baseline-capture.md`.

What the consult could not do and you must: run anything. No Lean, no
network to the Mathlib cache here. Every version pin is yours to take from
the Mathlib template; do not accept a version the consult might have
implied anywhere.

Gotcha: the `/home/sdowney/src/lean-graded` placeholder substitution is your first
action, before any worktree exists, because you are the only step that runs
in the main checkout.
