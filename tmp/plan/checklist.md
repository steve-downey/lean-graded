# Checklist — Lean model of graded Transpose

Ordinal is reading order; slug is identity. Cross-reference by slug only.
Mark your own step done before writing the next step's handoff.

- [x] 1. [baseline-capture](step-baseline-capture.md)
- [x] 2. [grade-pomonoid](step-grade-pomonoid.md)
- [x] 3. [graded-carrier](step-graded-carrier.md)
- [x] 4. [subsumption-widen](step-subsumption-widen.md)
- [x] 5. [monad-laws](step-monad-laws.md)
- [x] 6. [applicative-from-monad](step-applicative-from-monad.md)
- [x] 7. [applicative-accumulation](step-applicative-accumulation.md)
- [x] 8. [traverse-list](step-traverse-list.md)
- [x] 9. [traverse-tuple](step-traverse-tuple.md)
- [x] 10. [compose-flatten](step-compose-flatten.md) — partial: flattened traverse composition law refuted, product-graded case untested, see [graded-traversable-composition](../../docs/design.md#graded-traversable-composition)
- [x] 11. [graded-morphism](step-graded-morphism.md)
- [x] 12. [canonical-representation](step-canonical-representation.md)
- [x] 13. [compose-applicative](step-compose-applicative.md) — product-graded composition law (`traverseComp_eq`) holds unconditionally; `flatten_ap` holds only conditionally and explains [compose-flatten]'s counterexample; [graded-traversable-composition](../../docs/design.md#graded-traversable-composition) now CLOSED
- [x] 14. [ungraded-baseline](step-ungraded-baseline.md) — added 2026-09-08: the comparison column, what grading adds; monad/applicative laws cast-free at a fixed grade, idempotence only; `sumEquiv` transport to Mathlib's `Sum` proved; flattened composition re-confirmed false at a fixed grade
- [x] 15. [grade-obligations](step-grade-obligations.md) — added 2026-09-08: what a grade must be, layered, with a non-idempotent counter-instance; `foldG_le` needed `IsIdemPomonoid`, not `Pomonoid` alone as predicted — see [obligations](../../docs/design.md#obligations)
- [x] 16. [oracle-export](step-oracle-export.md) — added 2026-09-08: mechanical law inventory (146 theorems) with pomonoid properties and C++ probe list; corrected the plan's own predictions — commutativity is six sites not three, the at-most-one-error condition is four sites and not the same finding four times, `foldG_le`'s table row is honestly incomplete pending [grade-join-strength](../../docs/design.md#grade-join-strength)
- [x] 17. [blog-series-edit](step-blog-series-edit.md) — closing.org numbered Letter 16, not the literally-stated 17 (fixed a systematic off-by-one self-numbering bug in the existing letters instead); see [handoff-integration-review](handoff-integration-review.md)
- [x] 18. [integration-review](INTEGRATION-REVIEW.md) — cross-step coherence GREEN; findings in §6. The three doc/code defects it named (`join_comm` "second appearance" at five sites, `#provisional-decisions` missing `#grade`, two `Canonical.lean` rules violations, and an untested commutativity path) were fixed in `94bd0b8`; the `#carrier` cast verdict being overtaken by `ComposeApp` is addressed by steps 19-20 below. Metrics promoted to `metrics/fanout-runs.jsonl`

Follow-up run, planned 2026-09-08 after the integration review found the
cast burden had outgrown [monad-laws]'s verdict. Step 20 is gated on 19's
measured verdict; the remainder is the open question
[cast-burden-migration-scope](../../docs/design.md#cast-burden-migration-scope).

- [x] 19. [sufficient-grade-bind](step-sufficient-grade-bind.md) — `bindK` beside `bind`, cast-free laws, `bind_eq_bindK` bridge closes by `rfl`, `bindK_irrel` free by `rfl`; verdict: worth extending to the applicative layer
- [x] 20. [sufficient-grade-applicative](step-sufficient-grade-applicative.md) — `apK`/`map2K`/`Comp.apK` cast-free, `Comp.ap_interchange`'s cast-bearing analogue now zero; `apK_flip` needs no `Grade.join_comm` — commutativity was a cost of computing the grade exactly, not the applicative; see [cast-burden-migration-scope](../../docs/design.md#cast-burden-migration-scope)
- [x] 21. [obligation-layering](step-obligation-layering.md) — classified all 14 non-defining `join_comm`/`join_idem` consumers as canonicalization (grade-spelling), none operational; hypothesis holds — `Pomonoid` is the whole operational obligation; `IsCommPomonoid`/`IsIdemPomonoid` decoupled to siblings, `IsCanonicalPomonoid` added as the named bundle; `join_le`/`foldG_le` are grade-boundedness facts, not operational, but sharpen rather than close [grade-join-strength](../../docs/design.md#grade-join-strength)
- [x] 22. [sufficient-grade-traverse](step-sufficient-grade-traverse.md) — the fold disappeared entirely: no `foldGradeK`, every `traverseK` analogue cites no pomonoid property at all; `traverse_eq_traverseK` bridges cleanly, paying `Grade.join_idem` once on the union-graded side only
- [x] 23. [sufficient-grade-morphism](step-sufficient-grade-morphism.md) — the morphism leg; a grade morphism need only be monotone, not a homomorphism; the bridge from `GradedHom` is one-directional and stops at `gmap_mono` alone, since `hom` is never asked to commute with `widen`; built the monotone-but-not-homomorphism counter-instance
- [x] 24. [sufficient-grade-nested](step-sufficient-grade-nested.md) — the nested carriers; `flattenK`'s swap law is `rfl` with no hypothesis at all; `Comp.grade_reassoc` has no analogue (unnecessary) — the sufficient-grade layer now cites `Grade.join_comm` nowhere operationally; `flatten_apK`'s value-side hypothesis survives unchanged, checked against a concrete both-fail case; **closes** [cast-burden-migration-scope](../../docs/design.md#cast-burden-migration-scope)
- [x] 25. [migration-review](MIGRATION-REVIEW.md) — additive claim holds (0/57 casts, axioms clean, `flatten_apK` non-vacuous); built the `GradedHom → GradedHomK` lift in Lean and found it total under two hypotheses, so "records bridge differently" is retired; found the summary table summed to 39 not 38, `#laws-inventory` stale at 146, and the provisional index calling a closed question open; metrics promoted as `run: lean-model-2`

Improvement run, planned 2026-09-09 from
[lean-model-improvements](../../docs/plans/lean-model-improvements.md) and
the counter-plan that reviews it,
[lean-model-improvements-review](../../docs/plans/lean-model-improvements-review.md)
§5, whose lettered tranches are ordered by evidence per unit of churn.
Each tranche is gated on the previous one's green `make all`.

- [x] 26. [truth-in-labelling](../../docs/plans/lean-model-improvements-review.md#tranche-a--truth-in-labelling-no-new-lean-content) — Tranche A: removed three claims the repository asserted and had not proved (`foldGrade_cons_ne_nil` read as reachability, `canonEquiv` read as C++ type identity, `Pomonoid` documented as a partial order when its fields are a preorder). Prose only: 203 theorems before and after, `docs/laws.json` byte-identical, no theorem statement touched. The `Pomonoid` rename and an antisymmetry mixin are deferred to [grade-join-strength](../../docs/design.md#grade-join-strength), which needs a grade that separates preorder from partial order before either discriminates anything
- [x] 27. [grade-join-strength](../../docs/plans/lean-model-improvements-review.md#tranche-b--close-grade-join-strength-with-a-witness) — Tranche B: added `Pack Err` (`List Err` under `++`, ordered by membership), the pre-canonical `error_set` argument pack. It inhabits `PreorderedGradeMonoid` and `IsLubGrade` and refutes `IsPartialOrderGrade`, so it is the first grade in the model to separate preorder from partial order. `IsLubGrade`/`IsPartialOrderGrade` added as parameterised `Prop` mixins; `IsIdemGrade.of_lub_of_antisymm` and `IsLubGrade.of_idem` prove both directions, neither an instance; `join_self_equiv` states what the bound gives over a bare preorder. **Closes** [grade-join-strength](../../docs/design.md#grade-join-strength): commutativity and idempotence are what antisymmetry converts the least-upper-bound law into, which makes this question and [representation](../../docs/design.md#representation) one question. `Pomonoid` renamed `PreorderedGradeMonoid`, `IsCanonicalGrade` demoted from class to `abbrev`, and the `extends` → parameterised reversal carries an amendment at [RULES.md#amendments](../../docs/RULES.md#amendments). Inventory taught the `antisymmetry` property: 203 → 211 theorems, zero existing rows changed
- [x] 28. [accum-traverse](../../docs/plans/lean-model-improvements-review.md#tranche-c--the-two-cheap-missing-theorems) — Tranche C, first half: `Graded/AccumTraverse.lean`, a new module because `Accum` precedes `Morphism` precedes `Sufficient` and the commutation theorem needs `Graded.traverseK`. `Accum.apK` is primitive (no monad to derive it from) and consumes no order property where `Accum.ap` cites `Grade.le_join_left`/`le_join_right`. `errsOf_traverseK`: the accumulated error list is `xs.flatMap (errsOf ∘ f)`, so every failing position contributes in source order exactly once, at no pomonoid cost. `traverseK_ok`: success is `List.map`. **`toGraded_traverseK`**: first-error projection commutes with traversal unconditionally, lifting `toGraded_grade'` through the list. Guards run zero/one/two/three failures at two distinguishable kinds, both orderings
- [x] 29. [morphism-bridge](../../docs/plans/lean-model-improvements-review.md#tranche-c--the-two-cheap-missing-theorems) — Tranche C, second half: added `GradedHom.hom_widen`, one field, and `GradedHom.toGradedHomK` is **total**. `renameHomK` is now `(renameHom φ).toGradedHomK`, with `renameHomK_hom`/`renameHomK_gmap` recording by `rfl` that nothing changed; `GradedHom.hom_ok` (successes preserved at every grade, not just `bot`) falls out. Corrects [sufficient-grade-morphism]'s "the forward direction stops at `gmap_mono`" and [migration-review]'s "the lift needs two hypotheses": the payload-naturality obligation is forced by discharging `hom_bindK` with a case split, and the new `bindK_eq_widen_bind` (a fifth bridge, running from sufficient grade to computed union) avoids the split. No converse; `constHomK_not_gmap_bot` stands
- [x] 30. [coverage-enforcement](../../docs/plans/lean-model-improvements-review.md#tranche-d--coverage-enforcement-classified) — Tranche D: `scripts/test-coverage.py`, a companion pass over `laws-inventory.py`'s existing parse, classifying every theorem (counterexample / reduction / law) with one rule each; `scripts/axioms-check.py` runs `#print axioms` over all 171 law-class declarations and asserts the closure is `{propext, Classical.choice, Quot.sound}`. Both added to `make all` **alongside** `letters`, and so to CI, which needed no change. Wrote examples for the 38 genuinely uncovered declarations, to the fixture requirements now recorded at [RULES.md#tests](../../docs/RULES.md); **zero** exemptions, against a budget of twelve. Removed a last-segment name fallback that had credited `Comp.ap_ok_ok` to an example applying `Graded.ap_ok_ok`. Added `.github/workflows/cold-deps.yml`, weekly, asserting the lockfile survives a clean resolve
- [x] 31. [abstract-effects](../../docs/plans/lean-model-improvements-review.md#tranche-e--abstract-operational-classes-bet-with-an-exit) — Tranche E, the abstraction bet: `Graded/EffectK.lean` connects `Graded/Obligations.lean` to the carriers, which nothing had consumed before. `GradedFunctorK`/`GradedMonadK`/`GradedApplicativeK` with the latter two taking the functor as a *parameter*, one `pure`, `pureK` derived; laws in `Lawful*` companions. Instantiated at `Graded` (all three + all lawful), `Accum` (applicative, **provably no monad** — synthesis fails), and `Comp` (data only, via a new componentwise product grade instance). `traverseGK` defined once and shown to *be* both concrete traversals; `ApplicativeTransformationK` + `app_traverseGK` recover [accum-traverse]'s `toGraded_traverseK` as a generic law. **Exit criterion passed on both halves**: 29/29 instance fields are bare citations (zero scaffolding), elaboration under the threshold — see [abstract-operational-classes](../../docs/design.md#abstract-operational-classes), which also records that the criterion covers an *additive* layer and must be re-asked at Tranche G's migration. Two model gaps surfaced from instantiating: `Accum` had no `widen` and no sufficient-grade applicative laws; both added
- [x] 32. [payload-carrier](../../docs/plans/lean-model-improvements-review.md#tranche-f--payloads-largest-gated-on-e) — Tranche F, the carrier half: `Graded/Signature.lean` (`ErrorSignature`, `tagOnly`) and `ExpectedG` in `Graded/Carrier.lean`, with `Graded` **defined as** the tag-only specialization rather than proved equivalent to it. Decided by prototype before touching the tree, per the review's §3.5; the measured migration cost is **0 theorem statements changed, 0 transports, 4 constructor lemmas restated, 76 `cases` sites gaining `using Graded.rec'`**. Three mechanisms, each found by a failure: `@[match_pattern]` smart constructors, a `@[cases_eliminator, induction_eliminator]` two-case recursor (without which `cases … | err e he` silently binds the *payload*), and `Graded` as a `def` not an `abbrev` with its constructors in the type's own namespace. `DecidableEq`/`Repr` over `[∀ k, DecidableEq (S.Payload k)]` — the per-signature obligation every `#guard` reduces through. Universe pin for `tagOnly` carried as an amendment at [RULES.md#amendments](../../docs/RULES.md). `Examples/Payload.lean` demonstrates real payloads end to end. **Outstanding** (listed at [payloads](../../docs/design.md#payloads)): `Sigma`-payload accumulation, payload-preservation laws, `ErrorSignatureMap`, `CompleteGrade`, and porting `Examples/Validation.lean`
- [x] 33. [module-split](../../docs/plans/lean-model-improvements-review.md#tranche-g--modularize) — Tranche G: `Graded/Sufficient.lean`'s 1100 lines become seven modules under `Graded/Sufficient/` with the old name kept as a re-export shim, so no import changed and no theorem lost its name; `joinAll` extracted to `Graded/GradeFold.lean` so `Canonical` no longer imports the tuple development. **Boundary compiled, not asserted**: a file importing only `.MonadK` resolves `bindK` and fails on `traverseK`/`flattenK`/`GradedHomK`/`Comp.apK`; one importing only `Canonical` fails on `GList`/`sequence`. Added `sequenceK` (§3.7's gap — heterogeneous sequencing had no sufficient-grade sibling), with `sequence_eq_sequenceK` **left unproved and documented**: the two recursions descend through different grades. Law inventory gained a `layer` column and census (222 concrete / 28 generic / 5 representation / 52 C++ probe). **The split cost nothing**: 16 untouched test modules, 35985 heartbeats before and after, identical. **Found: `scripts/laws-inventory.py` globbed non-recursively and silently dropped 61 of 255 theorems while reporting `0 flagged`** — both scripts now `rglob`, and `test-coverage` learned to find a nested module's tests one directory up
